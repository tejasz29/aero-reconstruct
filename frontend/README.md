# Frontend placeholder (STEP 18 — React/Next.js + Three.js viewer).
#
# Planned features: upload video + GPS/metadata, processing progress,
# drone/GPS trajectory overlay, point-cloud / mesh / textured views,
# orbit-pan-zoom, point selection, distance + height measurement,
# confidence/coverage visualisation, export buttons.
#
# Nothing is implemented yet — backend endpoints (STEP 19) come first.
